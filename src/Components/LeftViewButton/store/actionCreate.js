import {CHANGESYNCSCROLL, CHANGEVIEWMOVECODE, CHANGEONEPAPER, CHANGEFONTFAMILY} from './constants';

export const change_SyncScroll = (data) =>({
  type: CHANGESYNCSCROLL,
})

export const change_ViewMoveCode = (data) =>({
  type: CHANGEVIEWMOVECODE
})